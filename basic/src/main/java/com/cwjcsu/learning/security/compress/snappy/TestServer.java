package com.cwjcsu.learning.security.compress.snappy;

import com.cwjcsu.learning.security.compress.Test;

import java.io.IOException;


/**
 * org.xerial.snappy.SnappyInputStream
 * @author atlas
 * @date 2012-9-7
 */
public class TestServer extends Test {
	public static void main(String[] args) throws IOException {
/*		int port = Integer.parseInt("1111");
		// Wait for a connection.
		ServerSocket ss = new ServerSocket(port);
		// System.out.println("Listening on port " + port + "...");
		Socket s = ss.accept();
		OutputStream out = s.getOutputStream();
		InputStream in = s.getInputStream();
		if (compressed) {
			out = new SnappyOutputStream(s.getOutputStream());
			in = new SnappyInputStream(s.getInputStream());
		}
		copy(in, out);*/
	}

}
